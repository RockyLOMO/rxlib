# 时间同步模块 (org.rx.net.ntp)

基于网络时间协议（NTP）实现的时间同步服务。

## 核心类介绍

- **`NtpClient`**:
  执行 NTP 请求的客户端工具。向远程 NTP 服务器（如 `pool.ntp.org`）发起 UDP 时间同步请求，计算并纠正本地时间偏移（Offset）。

- **`NtpPacket`**:
  NTP 协议数据包格式（RFC 5905）的 POJO 和编解码载体。

## 适用场景
- 需要分布式节点在内网中保持高度时间一致性（如分布式锁的超时判断或令牌桶限流算法）。
- 防止服务器本地时钟钟漂造成逻辑异常。
